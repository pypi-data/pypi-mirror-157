from setuptools import setup

setup(name='tim-gaussian',
      version='0.1',
      description='Gaussian and binomial distributions',
      author='Timothy Hobbs Kumah',
      packages=['tim-gaussian'],
      zip_safe=False)
