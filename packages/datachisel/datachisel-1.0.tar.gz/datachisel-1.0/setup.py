from setuptools import setup

setup(name='datachisel',
	version='1.0',
	description='an easy way to store data and load them in files',
	packages=['datachisel'],
	author='LeoDev (COSMICDEV)',
	zip_safe=False)